using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace FlockingAlgorithm1 {

	public class FlockWatcher : MonoBehaviour
	{		
		/*public Transform flockController;

		void LateUpdate()
		{

			if (flockController)
			{

				Vector3 watchPoint = flockController.GetComponent<FlockController>().flockCenterPosition;
				transform.LookAt(watchPoint + flockController.transform.position);
		}

	}*/
}
}
